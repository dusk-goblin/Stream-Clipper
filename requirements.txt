# Core runtime. Everything else is an optional extra -- see pyproject.toml.
PyYAML>=6.0
httpx>=0.27

# Optional stages. Uncomment what you need, or use the extras:
#   pip install -e ".[all]"
#
# streamlink>=6.7            # [capture]    live HLS pull
# faster-whisper>=1.0        # [whisper]    transcription
# sentence-transformers>=2.7 # [embeddings] semantic drift boundaries
# anthropic>=1.0             # [llm]        topic labels + clippability

# Dev
pytest>=8.0
pytest-asyncio>=0.23
